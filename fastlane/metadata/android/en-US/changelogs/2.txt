Add user-facing changes for the next release here.
- Colorful metric cards with icons for speed, heart rate, distance, energy, cadence, power, time, inclination, and resistance
- LAP/CHART workout view toggle shown during active recording
- LAP view with progress bar showing 1km circuit progress, lap time, and lap count
- Live line chart view for speed, inclination/resistance, and heart rate during workout
- Virtual FTMS treadmill available in debug builds for UI testing without a real device (DummyTreadmill)
- Bluetooth scan list now hides automatically when a workout starts (including when triggered by the treadmill or other device)
- Tapping a workout in History now opens a detailed view with line charts (speed + inclination for treadmill, cadence + resistance for indoor bike) plus time, distance, and calories summary
- Export and delete workout buttons moved to the detail view toolbar
- Debug Info moved to the overflow menu; removed from the main button row
- FTMS devices in the scan list now show an emoji matching the machine type (🏃 treadmill, 🚴 indoor bike, 🏋️ cross-trainer, 🪜 stair climber)
- Debug log export now shares only the current app session log
- Fixed BH Fitness treadmill elapsed time stuck at 0:02 — timer now advances correctly
- Inclination shown as whole number percent instead of one decimal place
- Fixed negative (decline) incline showing as large positive value
- Added BH treadmill fallback distance and energy estimation from speed/incline when counters are missing
- Fixed BH treadmill elapsed time fallback so timer keeps increasing when device sends zero
- Fixed BH treadmill timer offset by anchoring elapsed start to workout start signal (e.g. 0:02), not connection
- Fixed BH indoor bike metrics by deriving distance and energy from live bike packets
- Reduced occasional BH indoor bike cadence spikes by filtering outlier packets
- Added BH indoor bike resistance level detection (level 1..11) from live packet data
- Hidden BH indoor bike speed display because it duplicates stride-equivalent data
- Added treadmill speed and inclination control popups on metric tiles with slider and quick +/- buttons
- Added safety danger-zone indication while selecting high speed or extreme incline values
- Fixed treadmill session timer offset so app elapsed time aligns with real machine movement start
- Device scan list now correctly shows "HR" label for devices whose HR capability is confirmed at connection time (e.g. Mi Band advertising HR via fee0 instead of the standard 0x180D UUID)
- Added Heart Rate Control Point command on connect to start continuous HR streaming on devices that require it (e.g. some Mi Band variants)
- Added support for Mi Band 3 and similar devices that expose heart rate via a proprietary BLE service (fee0); they now appear as HR sensors and connect automatically
- Fixed Mi Band heart rate not received when connecting from the paired devices list: the app now auto-detects the device type from cached or live service discovery instead of requiring a manual dialog choice
- History button moved to the toolbar; scan button is now a compact Bluetooth icon
- App now auto-starts Bluetooth scan on launch when permissions are already granted
- Laps table in workout detail now shows km distance column header and includes the final partial km lap
- Scan button now uses a refresh icon with a visible background; icon spins while scanning is active
- Heart rate is now shown as a red line in the workout chart when HR data is available
- FIT export now records one lap per km (plus the final partial km) with per-lap speed, HR, cadence, and power averages
- Reduced recording frequency: samples saved at most every 2 seconds; unchanged data saved at most every 5 seconds
